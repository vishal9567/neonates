<section style="margin-top: 150px;" id="orderDiv">
<center><h3 style="color: rgb(236,79,162);">Wallet History</h3></center>
    <div class="container" style="margin-top: 30px;">
        <!-- <form action="/admin/searchUser" method="post">
            <div class="input-group mb-4">
                <input id="searchVal" name="user_search" type="search" placeholder="Search here..." aria-describedby="button-addon5"
                    class="form-control">
                <div class="input-group-append">
                    <button id="button-addon5" type="submit" class="btn btn-primary"> <i class="fa fa-search">Search</i> </button> 
                </div>
            </div>
        </form> 
        {{#if searchFailed}}
        <center class="text-danger bg-warning">No such entry</center>
        {{/if}}-->
        <p class="text-success">Wallet Amount: <span class="text-dark fw-bold">&#x20b9; {{history.[0].wallet}}</span></p>
        <div class="d-flex justify-content-between">
        
         <!-select-->
         <!-- <div class="d-flex">
            <p>Show</p>
          <div class="form-outline mb-2">
            <select id="limits" name="category" class="form-select px-3 py-1">
              
              <option>5</option>
              <option>10</option>
              <option>15</option>
              
            </select>
          </div>
          <p>entries</p>
          </div> -->
          <!-select-->
        </div>
        <table class="table table-bordered text-center" id="productsTable">
            <thead style="background-color: rgb(236,79,162)">
                <tr>
                    <th>Amount</th>
                    <th>Payment Type</th>
                    <th>Cash back reason</th>
                    <th>Date</th>
                </tr>
            </thead>
            {{!-- <tfoot>
                <tr>
                    <th>Name</th>
                    <th>Email</th>
                    <th>Status</th>
                    <th>Options</th>
                </tr>
            </tfoot> --}}
            {{#each history}}
            <tbody>
                <tr style="background-color: rgb(242, 197, 210);">
                    <td>{{this.wallethistory.amount}}</td>
                    <td>{{this.wallethistory.payType}}</td>
                    <td>   <div>
                            {{#if (compare this.wallethistory.status '==' 4)}} <p>Cancelled Order</p>{{/if}}
                            {{#if (compare this.wallethistory.status  '==' 3)}} <p>Cancelled Order</p>{{/if}}
                        </div></td>
                    <td>{{this.wallethistory.date}}</td>
                   
                </tr>

            </tbody>
            {{/each}}
        </table>
        <h6 class="text-center" id="notFound"></h6>
        <div>
            <!--pagination-->
            <nav aria-label="...">
                <ul class="pagination justify-content-center" id="pagination">

                     {{#for pages}}                   
                    <li class="page-item" aria-current="page">
                        <a class="page-link" href="/walletHistory?page={{this}}">{{this}}<span
                                class="visually-hidden">(current)</span></a>
                    </li>
                     {{/for}}

                </ul>
            </nav>
        </div>
    </div>
    <div class="container d-flex justify-content-end">
        <sub class="fas fa-long-arrow-alt-left me-2 text-success"></sub><a class="text-dark " href="/userDashBoard">Back
            to account</a>
    </div>
</section>
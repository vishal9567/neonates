<div class="container mt-5" style="width: 400px ;">
<form {{#if updating}}action="/admin/submitUpdate/{{coupon._id}}" method="post"{{else}}action="/admin/submitCoupon" method="post"{{/if}}>
  <!-- 2 column grid layout with text inputs for the first and last names -->
  <div class="row mb-4">
    <div class="col">
      <div class="form-outline">
        <input type="text" id="form3Example1" class="form-control" name="coupenName"{{#if updating}}value="{{coupon.coupenName}}"{{/if}} />
        <label class="form-label" for="form3Example1">Coupon Name</label>
      </div>
    </div>
    <div class="col">
      <div class="form-outline">
        <input type="text" id="form3Example2" class="form-control" name="discount" {{#if updating}}value="{{coupon.discount}}"{{/if}}/>
        <label class="form-label" for="form3Example2">Discount</label>
      </div>
    </div>
  </div>

  <!-- Email input -->
  <div class="form-outline mb-4">
    <input type="date" id="form3Example3" class="form-control" name="expiry" {{#if updating}}value="{{coupon.expiry}}"{{/if}}/>
    <label class="form-label" for="form3Example3">Date</label>
  </div>
  <div class="form-outline mb-4">
    <input type="number" id="form3Example4" class="form-control" name="minimum" {{#if updating}}value="{{coupon.minimum}}"{{/if}}/>
    <label class="form-label" for="form3Example4">Minimum Amount</label>
  </div>
  {{#if coupenCheck}} <p>Already Exist</p>{{/if}}

  <!-- Password input -->


  <!-- Checkbox -->
  

  <!-- Submit button -->
  <button type="submit" class="btn btn-primary btn-block mb-4">{{#if updating}}Update{{else}}Add{{/if}}</button>
  <center><a class="mt-2" href="/admin/coupenList">Go to...</a></center>

  <!-- Register buttons -->
  
</form>
</div>
    {{!--
</div> --}}
<script>

        let form = document.querySelector('form')
        let coupon = document.getElementById('form3Example1')
        let dis = document.getElementById('form3Example2')
        let ex = document.getElementById('form3Example3')
        let min = document.getElementById('form3Example4')

        form.addEventListener('submit', validation)

        function validation(e) {
            if (coupon.value == "") {
                e.preventDefault();
            }
            if (dis.value == "") {
                e.preventDefault()
            }
            if (ex.value == "") {
                e.preventDefault()
            }
            if (min.value == "") {
                e.preventDefault()
            }
        }
        const regexDate = /^(?:(?:31(\/|-|\.)(?:0?[13578]|1[02]|(?:Jan|Mar|May|Jul|Aug|Oct|Dec)))\1|(?:(?:29|30)(\/|-|\.)(?:0?[1,3-9]|1[0-2]|(?:Jan|Mar|Apr|May|Jun|Jul|Aug|Sep|Oct|Nov|Dec))\2))(?:(?:1[6-9]|[2-9]\d)?\d{2})$|^(?:29(\/|-|\.)(?:0?2|(?:Feb))\3(?:(?:(?:1[6-9]|[2-9]\d)?(?:0[48]|[2468][048]|[13579][26])|(?:(?:16|[2468][048]|[3579][26])00))))$|^(?:0?[1-9]|1\d|2[0-8])(\/|-|\.)(?:(?:0?[1-9]|(?:Jan|Feb|Mar|Apr|May|Jun|Jul|Aug|Sep))|(?:1[0-2]|(?:Oct|Nov|Dec)))\4(?:(?:1[6-9]|[2-9]\d)?\d{2})$/
    

</script>